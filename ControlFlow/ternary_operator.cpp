#include <iostream>

int main(){
    using namespace std;

    // ternary operator ?: = replacement to an if/else statement
    // condition ? expression1 : expression2;

    // int grade = 75;
    // grade >= 60 ? cout<<"You pass" : cout<<"You Fail";

    // int number =8;
    // number % 2 ? cout<<"ODD" : cout<<"EVEN";

    // bool hungry = true;
    // hungry ? cout<<"I am hungry" : cout<<"I am not hungry";
    // cout<<hungry ? "I am hungry" : "I am not hungry"; // return 1 or 0
    // cout<<(hungry ? "I am hungry" : "I am not hungry"); // return sentences
    return 0;
}